---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('blog'))
  .filter(post => !post.data.draft)
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());

const categoryNames: Record<string, string> = {
  'people': 'People',
  'discovery': 'Discovery',
  'making': 'Making',
  'jeju': 'Jeju',
  'notice': 'Notice',
};

const heroPost = posts[0];
const restPosts = posts.slice(1);

function formatDate(date: Date) {
  return date.toLocaleDateString('ko-KR', { year: 'numeric', month: '2-digit', day: '2-digit' }).replace(/\./g, '.').replace(/\s/g, ' ');
}
---

<BaseLayout title="Home">
  <div class="main-content">

    {/* HERO POST */}
    {heroPost && (
      <article class="hero-post">
        <a href={`/blog/${heroPost.id}`}>
          {heroPost.data.thumbnail ? (
            <div class="hero-post-image">
              <img src={heroPost.data.thumbnail} alt={heroPost.data.title} />
            </div>
          ) : (
            <div class="hero-post-image-placeholder"></div>
          )}
          <div class="hero-post-overlay">
            <span class="hero-post-category">
              {categoryNames[heroPost.data.category] || heroPost.data.category}
            </span>
            <h2 class="hero-post-title">{heroPost.data.title}</h2>
            <p class="hero-post-desc">{heroPost.data.description}</p>
            <div class="hero-post-meta">
              <span class="hero-post-author">{heroPost.data.author}</span>
              <time>{formatDate(heroPost.data.date)}</time>
            </div>
          </div>
        </a>
      </article>
    )}

    {/* POST LIST */}
    <div class="post-list">
      {restPosts.map((post, index) => {
        const isFeature = index > 0 && index % 4 === 3;
        const isOrange = index % 2 === 1;

        {/* 제품 발견 모듈 : 2번째 글 뒤에 삽입 */}
        if (index === 2) {
          return (
            <>
              <article class={`post-item ${isOrange ? 'orange' : ''}`}>
                <a href={`/blog/${post.id}`}>
                  {post.data.thumbnail ? (
                    <div class="post-item-image">
                      <img src={post.data.thumbnail} alt={post.data.title} />
                    </div>
                  ) : (
                    <div class="post-item-image-placeholder"></div>
                  )}
                  <div class="post-item-body">
                    <span class="post-item-category">
                      {categoryNames[post.data.category] || post.data.category}
                    </span>
                    <h2 class="post-item-title">{post.data.title}</h2>
                    <p class="post-item-desc">{post.data.description}</p>
                    <div class="post-item-footer">
                      <span class="post-item-author">{post.data.author}</span>
                      <time class="post-item-date">{formatDate(post.data.date)}</time>
                    </div>
                  </div>
                </a>
              </article>

              {/* PRODUCT DISCOVERY MODULE */}
              <div class="product-discover">
                <div class="product-discover-visual">
                  <div style="text-align:center;color:rgba(255,255,255,0.5);font-size:3rem;font-weight:900;letter-spacing:-0.03em;">귤박수</div>
                </div>
                <div class="product-discover-body">
                  <span class="product-discover-label">From Waste to Beauty</span>
                  <h2 class="product-discover-title">귤박수 3-STEP 기초 세트</h2>
                  <p class="product-discover-desc">
                    제주 감귤 착즙 후 버려지는 감귤박에서 추출한 시트러스 워터.
                    INCI 국제 등재 원료로 만든 100% 비건 클린뷰티 기초 라인을 만나보세요.
                  </p>
                  <a href="/product" class="product-discover-cta">자세히 보기</a>
                </div>
              </div>
            </>
          );
        }

        if (isFeature) {
          return (
            <article class="feature-post">
              <a href={`/blog/${post.id}`}>
                {post.data.thumbnail ? (
                  <div class="feature-post-image">
                    <img src={post.data.thumbnail} alt={post.data.title} />
                  </div>
                ) : (
                  <div class="feature-post-image-placeholder"></div>
                )}
                <div class="feature-post-overlay">
                  <span class="feature-post-category">
                    {categoryNames[post.data.category] || post.data.category}
                  </span>
                  <h2 class="feature-post-title">{post.data.title}</h2>
                  <p class="feature-post-desc">{post.data.description}</p>
                  <div class="feature-post-meta">
                    <span>{post.data.author}</span>
                    <time>{formatDate(post.data.date)}</time>
                  </div>
                </div>
              </a>
            </article>
          );
        }

        return (
          <article class={`post-item ${isOrange ? 'orange' : ''}`}>
            <a href={`/blog/${post.id}`}>
              {post.data.thumbnail ? (
                <div class="post-item-image">
                  <img src={post.data.thumbnail} alt={post.data.title} />
                </div>
              ) : (
                <div class="post-item-image-placeholder"></div>
              )}
              <div class="post-item-body">
                <span class="post-item-category">
                  {categoryNames[post.data.category] || post.data.category}
                </span>
                <h2 class="post-item-title">{post.data.title}</h2>
                <p class="post-item-desc">{post.data.description}</p>
                <div class="post-item-footer">
                  <span class="post-item-author">{post.data.author}</span>
                  <time class="post-item-date">{formatDate(post.data.date)}</time>
                </div>
              </div>
            </a>
          </article>
        );
      })}
    </div>

    {/* NEWSLETTER CTA */}
    <div class="newsletter-cta">
      <h2 class="newsletter-cta-title">제주에서 시작되는 이야기, 놓치지 않으려면.</h2>
      <p class="newsletter-cta-desc">이메일을 남겨주세요. 제주랩스의 새로운 이야기가 찾아갑니다.</p>
      <form
        class="newsletter-cta-form"
        name="newsletter"
        method="POST"
        data-netlify="true"
        data-netlify-honeypot="bot-field"
      >
        <input type="hidden" name="form-name" value="newsletter" />
        <input type="hidden" name="bot-field" />
        <input type="email" name="email" placeholder="이메일 주소를 입력하세요" required />
        <button type="submit">구독하기</button>
      </form>
    </div>

  </div>
</BaseLayout>
